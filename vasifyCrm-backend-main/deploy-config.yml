# Application settings
app_name: vasify-crm-backend
app_port: 8005
health_check_path: "/"
env: production

# Domain configuration
domain: crm-api.vasifytech.com

# Target machine (must match inventory.yml)
target_host: vt-prod-app-02

# SSL settings
ssl_email: yogendra@vasifytech.com

# Docker registry (GitLab)
registry: registry.gitlab.com
registry_image: registry.gitlab.com/vasify/client-projects/vasifycrm-backend
